import { actions, sagas, selectors, reducer } from './model';
import { config } from './config';

/** Модель пагинации */
export const filterModel = {
  config,
  actions,
  sagas,
  selectors,
  reducer,
};
